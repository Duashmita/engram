google-genai>=1.0.0
numpy>=1.24.0

# Optional: SWI-Prolog must be installed first (brew install swi-prolog)
# before pyswip will work. Without it, memory strength falls back to Python.
# pyswip>=0.2.10
