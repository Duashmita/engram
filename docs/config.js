// docs/config.js
// After running `modal deploy backend/modal_app.py`, paste the printed URL here.
// For local dev with `uvicorn backend.modal_app:api --port 8000`, the default below works.
export const BACKEND_URL = "https://duashmita--engram-demo-fastapi-app.modal.run";
