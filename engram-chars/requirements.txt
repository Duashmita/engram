anthropic>=0.40.0
voyageai>=0.3.0
numpy>=1.24.0
requests>=2.31.0

# Optional: SWI-Prolog must be installed first (brew install swi-prolog)
# before pyswip will work. Without it, contradiction checking degrades gracefully.
# pyswip>=0.2.10
