# Secrets — never commit
.env
.claude

# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/

# Runtime / generated
data/
.llm_cache/
docs/sessions/
.DS_Store

# Character GLBs ARE committed (preloaded preset models). Re-create any with
# scripts/generate_characters.py. Only ignore each preset's resumable state file.
docs/assets/characters/*/state.json
